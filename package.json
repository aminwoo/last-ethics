{
  "name": "top-down-shooter",
  "version": "1.0.0",
  "description": "A top-down shooter game built with Three.js",
  "main": "src/main.js",
  "scripts": {
    "test": "node --experimental-vm-modules --test tests/*.test.mjs",
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "node server/server.js",
    "serve:build": "npm run build && node server/server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "three": "^0.162.0",
    "ws": "^8.18.1"
  },
  "devDependencies": {
    "vite": "^5.4.14"
  },
  "engines": {
    "node": ">=20"
  }
}
